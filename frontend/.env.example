# ── Badminton Tournament Frontend — Environment Variables ─────────────────
# Copy this file to .env and fill in your values.

# API base URL — if running via Vite proxy, leave as /api
# For production point to your deployed API
VITE_API_URL=/api
